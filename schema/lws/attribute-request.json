{
  "$schema": "http://json-schema.org/draft-04/schema",
  "id": "http://platform.selfkey.org/schema/lws/attribute-request.json",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description":
        "A human-readable name for the attribute request, used for labeling the attribute in a UI context"
    },
    "key": {
      "type": "string",
      "description":
        "A machine-readable name for the attribute, used for identifying the attribute data in the identity response"
    },
    "attribute": {
      "type": "string",
      "description": "Path to a JSON schema defining the attribute data."
    }
  }
}
